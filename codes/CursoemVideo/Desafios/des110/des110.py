from moeda import resumo

p = float(input('Digite o preço: R$'))
aumento = float(input('Digite o valor percentual de aumento: '))
redução = float(input('Digite o valor percentual de redução: '))
resumo(p, aumento, redução)
